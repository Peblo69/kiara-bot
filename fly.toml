app = "kiara-bot"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

# Discord bots don't need HTTP - no services or health checks
[processes]
  app = "python bot.py"

# No HTTP services needed for Discord bot
[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
